import React, { Fragment } from 'react';
import Typography from '@material-ui/core/Typography'

const Header = () => (
        <Fragment>
            <Typography variant="h5" align="center">
                Frameworks as Linked Data!
            </Typography>
       </Fragment>
);

export default Header;
